# drone_experiment 协作约定

## 两个项目子智能体

用户要求本项目使用导师与组员协作。角色定义保存在：

- **Mentor**（调用名 `mentor`）：`.codex/agents/mentor.toml`。独立思考、监督研究、审查方案与成果，保持只读。
- **member**（调用名 `member`）：`.codex/agents/member.toml`。实施用户需求及授权范围内的导师建议，并提供验证结果。

主智能体负责理解需求、划定任务范围、协调文件所有权、综合意见和向用户汇报。导师不是用户的真实教授，其建议不自动扩大用户授权。

## 调用与分工

对于有实质研究判断、代码实施、数据分析、训练或论文产出的任务，使用这两个角色协作；简单问答和状态查询不必为了分工重复启动任务。

1. 主智能体先说明目标、现有约束和验收标准，将有独立价值的具体子任务交给 Mentor 和 member。
2. Mentor 独立评价方法与风险；member 可以同时开展只读检查或已明确、互不依赖的实施工作。依赖导师结论的修改必须等结论明确后再做。
3. 主智能体把范围内的导师建议转为 member 的实施任务。只有需要扩大授权或改变用户研究选择时才请用户决定，不要求每个普通修复都重复确认。
4. member 完成后，Mentor 对实际产物和验证证据独立复核。主智能体处理分歧、必要返工并给出统一结果；不隐瞒双方尚未解决的分歧。
5. 不让多个智能体同时编辑同一文件。导师不编辑项目；主智能体与 member 明确各自负责的文件。

使用支持自定义角色的接口时选择对应角色。若当前协作接口只能指定子任务而不能选择角色，启动前读取对应 TOML，将完整 `developer_instructions` 作为角色任务约定传给子智能体，并明确本次范围及只读/实施限制；不要声称接口未实际应用的权限已被强制执行。

这些调用规则仅由主智能体执行。已被指定为 Mentor 或 member 的子智能体不得再次套用上述流程生成下级智能体。没有具体任务时不启动空转的子智能体，也不默认创建常驻监控或定时任务。

## 共同研究与安全边界

- 默认中文沟通；英文论文遵循当前原文语气和格式。
- 围绕用户当前明确的目标工作：minimizing the total time。每次分析核实输入、约束、优化范围和时间定义。
- 先检查真实文件、代码和数据，不把历史对话里的旧参数当成当前配置；数据来源、实验类型、电池归一化及 SOC 定义必须一致。
- 保留原始实验、失败记录及用户已有修改。派生处理应可追溯；实测与模拟、插值、外推必须可区分。
- 不伪造数据、引用或验证结果。论文中的计算结果与物理解释分别表述，引用应支持相应论点。
- 用户只问原因或要求审查时不修改代码。未获得当前任务明确授权，不操纵无人机、不启动实机飞行，不对外发布、Git push 或删除重要数据。
- 验证优先使用明确无硬件副作用的离线检查。不得把离线测试通过写成实机已经安全验证。
- 子智能体默认继承当前模型选择，不擅自指定不同模型或推理强度。所有角色仍遵守当前系统和开发者指令。
